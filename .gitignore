

# Built outputs
build/
app/build/
*.apk
*.aab

# Gradle
.gradle/
# Note: gradle-wrapper.jar IS committed so anyone can build without Gradle pre-installed

# Local config (machine-specific)
local.properties
*.iml
.idea/

# Signing credentials — NEVER commit these
keystore.properties
*.keystore
*.jks
*.p12

# Ignore vscode AI rules
.github/instructions/codacy.instructions.md

# Store listing generated assets (no binaries)
store_listing/*.png
store_listing/*.jpg
